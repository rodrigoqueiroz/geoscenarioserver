behaviortree walk_tree:
    ?
        ->
            condition c_goal ( reached_goal( threshold=1 ) )
            maneuver m_stop_goal ( MStopConfig( type=2 ) )
        ->
            condition c_curr_dest ( reached_next_waypoint( threshold=1 ) )
            maneuver m_waypt_next ( MUpdateWaypoint( vel=MP(1.0,30,6) ) )
        maneuver m_keep_velocity ( MVelKeepConfig( vel=MP(1.0,30,6) ) )

