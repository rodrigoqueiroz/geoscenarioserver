behaviortree walk_tree:
    ?
        ->
            condition c_goal ( reached_goal( threshold=1.5 ) )
            maneuver m_stop_goal ( MStopConfig( type=2 ) )
        ->
            condition c_curr_dest ( reached_curr_destination( threshold=0.5 ) )
            maneuver m_dest_next ( MUpdateDestination( vel=MP(1.0,30,6) ) )
        maneuver m_keep_velocity ( MVelKeepConfig( vel=MP(1.0,30,6) ) )

