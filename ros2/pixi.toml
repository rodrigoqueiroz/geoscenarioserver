# GeoScenario ROS2 Conda Package
#
# This creates a single conda package containing all ROS2 nodes.

[package]
name = "ros-humble-geoscenarioserver"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-rattler-build", version = "*" }
