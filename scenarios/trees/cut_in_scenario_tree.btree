behaviortree LaneChange:
    ?
        ->
            condition trigger( sim_time (repeat=True, tmin=3))
            ?
                ->
                    condition reached_gap(reached_gap(repeat=False, target_lane_id=-1, meters=2))
                    maneuver cutin(MCutInConfig(target_lid=-1, delta_s=tuple((20, 5, 0))))
                subtree drive_tree(keep_velocity=MVelKeepConfig(vel=MP(12.0,10,6)))
        maneuver stop(MStopConfig())
