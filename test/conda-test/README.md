# geoscenarioserver conda package installation and execution test

# pixi 

The workspace definition in `pixi.yaml` contains pypi-dependencies that cannot be included in the conda package.

# micromamba

For micromamba, we create a local channel from which the package can be installed in `repo/`.
